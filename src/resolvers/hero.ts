import { HeroResolvers, Hero as IHero } from '../models';

export const Hero: HeroResolvers.Resolvers = {
  // TODO: this should be optional
  // id: (hero: any) => hero.id,
  // TODO: this should be optional
  // name: (hero: any) => hero.name,
};
